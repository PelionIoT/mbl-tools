- from: inline
  repository:
    metadata:
      format: Lava-Test Test Definition 1.0
      name: install-pip3
      description: "install-pip3"
    run:
      steps:
        # Install the prerequsite package
        - apt-get install -q -q --yes curl ca-certificates

        # Install pip3
        - curl https://bootstrap.pypa.io/get-pip.py | python3
  name: install-pip3
  path: inline/install-pip3.yaml


