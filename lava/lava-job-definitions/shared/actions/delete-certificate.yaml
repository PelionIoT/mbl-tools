- from: inline
  repository:
    metadata:
      format: Lava-Test Test Definition 1.0
      name: delete-certificate
      description: "delete-certificate"
    run:
      steps:
        - {{ venv_name }}/bin/mbl-cli delete-certificate $(pwd | cut -d"/" -f2)
  name: delete-certificate
  path: inline/delete-certificate.yaml
